import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './rekain-store'

ReactDOM.createRoot(document.getElementById('root')).render(
  <div style={{padding:40, fontsize:24}}>
    React Jalan
    </div>
)
